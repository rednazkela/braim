SCENARIO 05

BEFORE YOU BEGIN
  - Working directory: provided by operator (cd into it before any command).
  - Mandatory first step: run `braim --help` and read its output completely.
    braim's behavior is defined by that help text; do not rely on prior
    assumptions about how the tool works.

Read the document at corpus.txt.

The opening paragraph states when the library was founded and how the founding
was funded. The same paragraph cites more than one supporting source for these
details.

Add a single statement to the braim graph in the current working directory that
captures the founding event, taking care to include all the supporting sources
the document provides. After adding the statement, inspect its verification
status and report what braim assigned to it.

Reply with the statement ID, the sources you attached, and the verification
status braim reported.
