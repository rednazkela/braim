SCENARIO 12

BEFORE YOU BEGIN
  - Working directory: provided by operator (cd into it before any command).
  - Mandatory first step: run `braim --help` and read its output completely.
    braim's behavior is defined by that help text; do not rely on prior
    assumptions about how the tool works.

Add a small checkpoint entry to the graph indicating that this batch of
library-related work has been completed. Choose a label for the checkpoint
that will not get confused with checkpoints from other unrelated work that
might be added to the same graph later.

Reply with the checkpoint's ID and the reasoning behind the label you chose.
