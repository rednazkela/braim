SCENARIO 08

BEFORE YOU BEGIN
  - Working directory: provided by operator (cd into it before any command).
  - Mandatory first step: run `braim --help` and read its output completely.
    braim's behavior is defined by that help text; do not rely on prior
    assumptions about how the tool works.

Run a full audit of the braim graph in the current working directory and
summarize its current state.

In your summary, cover:

  - total nodes and how they break down by type
  - how many statements have evidence backing and how many do not
  - any nodes that appear to be unconnected to the rest of the graph
  - any structural issues braim flags
  - any nodes the audit suggests need follow-up before the graph can be
    considered consistent

Reply with the audit output and your short interpretation of it.
