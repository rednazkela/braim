SCENARIO 09

BEFORE YOU BEGIN
  - Working directory: provided by operator (cd into it before any command).
  - Mandatory first step: run `braim --help` and read its output completely.
    braim's behavior is defined by that help text; do not rely on prior
    assumptions about how the tool works.

The graph already includes several entries about the library's membership
offerings: the membership tiers, the privileges each tier grants, and so on.

Compose a single higher-level concept that represents the overall membership
system this library runs. This new concept should draw together the relevant
existing entries so a future reader can pivot from the system as a whole to
the parts that make it up.

Reply with the new concept's ID, the entries you chose to draw from, and the
rationale for that selection.
