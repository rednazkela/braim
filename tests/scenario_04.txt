SCENARIO 04

BEFORE YOU BEGIN
  - Working directory: provided by operator (cd into it before any command).
  - Mandatory first step: run `braim --help` and read its output completely.
    braim's behavior is defined by that help text; do not rely on prior
    assumptions about how the tool works.

Read the document at corpus.txt.

The document makes several factual claims about how the library operates: who
issues cards, what a loan period is, what a Late Fee costs, who is responsible
for which duties, and so on. Identify the substantive claims in the text and
add each one as a statement in the braim graph in the current working
directory, linking the concepts each claim involves.

When adding a statement, cite the parts of the document that support it. Make
sure the sources you cite are attributed in a form that braim recognizes.

Reply with the list of statement IDs you created.
