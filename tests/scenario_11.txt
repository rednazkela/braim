SCENARIO 11

BEFORE YOU BEGIN
  - Working directory: provided by operator (cd into it before any command).
  - Mandatory first step: run `braim --help` and read its output completely.
    braim's behavior is defined by that help text; do not rely on prior
    assumptions about how the tool works.

Capture the rule the document states about what happens when a borrower keeps
a book longer than the allowed period. Record this rule as a single entry in
the graph, linking it to whatever existing entries it naturally relates to.

Cite the supporting passages in the document.

Reply with the entry's ID, the kind of entry you chose, and the entries it is
linked to.
